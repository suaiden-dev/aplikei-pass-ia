# Tasks - 09/06/2026

Repositório: `/home/vileladev/Projects/aplikei`

Objetivo do dia: separar regra de negócio e chamadas de API/Supabase das páginas, movendo para `services` e organizando tipos por feature. Também foram analisados e removidos comentários obsoletos/código morto.

---

## 1. Organização De Types Por Feature

- Criados/ajustados arquivos `types.ts` por feature.
- Incluídos entrypoints de types para:
  - `admin`
  - `customer`
  - `legal`
  - `marketing`
  - `onboarding`
  - `page-builder`
  - `seller`
  - `system`
  - `offices/types`
- Ajustados exports e tipos usados pelos services extraídos.

Status: concluído.

---

## 2. Refatorações De Baixa Dificuldade

### 2.1 Legal

- Criado `src/features/legal/services/legalTermsService.ts`.
- Movidas chamadas de termos legais para service.
- Limpas páginas:
  - `Legal Terms`
  - `Privacy`
  - `Terms`

Status: concluído.

### 2.2 Admin - Legal Terms E Lawyers

- Criado `src/features/admin/services/lawyersService.ts`.
- Refatorada `LawyersPage`.
- Refatorada `LegalTermsPage`.
- Corrigido uso do TipTap em `RichEditor` para API atual de `setContent`.

Status: concluído.

### 2.3 Offices

- Expandido `src/features/offices/services/officeOps.ts`.
- Movidas operações de detalhes e estatísticas de offices para service.
- Limpas:
  - `OfficesPage`
  - `OfficeDetailsPage`

Status: concluído.

### 2.4 Auth E Customer

- `authService` recebeu funções para:
  - buscar logo do office;
  - atualizar email.
- Limpas:
  - `LoginPage`
  - `ProfileSettingsPage`

Status: concluído.

---

## 3. Refatorações Admin De Baixa/Média

### 3.1 Payment Settings

- Criado `src/features/admin/services/paymentSettingsService.ts`.
- Removidas chamadas diretas da página de configurações de pagamento.

Status: concluído.

### 3.2 Withdrawals

- Criado `src/features/admin/services/withdrawalsService.ts`.
- Movidas operações de saques para service.

Status: concluído.

### 3.3 Subscription Plans

- Criado `src/features/admin/services/subscriptionPlansService.ts`.
- Movidas operações de planos para service.

Status: concluído.

### 3.4 Discount Rules

- Criado `src/features/admin/services/discountRulesService.ts`.
- Movidas regras de desconto para service.

Status: concluído.

### 3.5 Customers

- Expandido `src/features/admin/services/adminCustomerService.ts`.
- Adicionado carregamento de clientes com estatísticas.
- Ajustada tipagem de status para evitar erro no `typecheck`.

Status: concluído.

---

## 4. Refatorações Onboarding De Baixa/Média

### 4.1 Storage Do Onboarding

- Criado `src/features/onboarding/services/onboardingStorageService.ts`.
- Centralizadas operações de upload e URL pública para documentos do onboarding.

Status: concluído.

### 4.2 Process Data Do Onboarding

- Criado `src/features/onboarding/services/onboardingProcessDataService.ts`.
- Movidas operações de leitura/atualização de dados de processo.

Status: concluído.

### 4.3 Steps Limpas

Foram limpas chamadas diretas em:

- `F1I20UploadStep`
- `B1B2UserReviewSignStep`
- `B1B2MRVPaymentStep`
- `I20UploadStep`
- `SevisFeeStep`
- `I539FormStep`
- `useStepInitialInfo`

Status: concluído.

---

## 5. Refatorações De Média Dificuldade

### 5.1 Interaction Logs

- Criado `src/features/admin/services/interactionLogsService.ts`.
- Refatorada `InteractionLogsPage`.
- Corrigido import de `toast` após validação de types.

Status: concluído.

### 5.2 Seller Earnings

- Populado `src/features/seller/types.ts`.
- Criado `src/features/seller/services/earningsService.ts`.
- Refatorada `EarningsPage`.

Status: concluído.

### 5.3 Process Office Names

- Criado `src/features/process/services/officeNamesService.ts`.
- Refatorada `MyProcessesPage`.

Status: concluído.

### 5.4 Page Builder

- Criado service para persistência/storage do Page Builder.
- Refatorado `usePageBuilder`.

Status: concluído.

### 5.5 Address Lookup

- Criado `src/features/onboarding/services/addressLookupService.ts`.
- Refatorados pontos que faziam busca de endereço/CEP/ZIP.

Status: concluído.

---

## 6. Refatorações Média/Alta

### 6.1 Stripe Connect

- Criado `src/features/admin/services/stripeConnectService.ts`.
- Movidas operações:
  - callback do Stripe Connect;
  - criação de URL de conexão;
  - desconexão da conta Stripe.
- Refatorada `PaymentMethodsSettingsPage`.

Status: concluído.

### 6.2 Subscription Page

- Criado `src/features/admin/services/subscriptionPageService.ts`.
- Movidas operações:
  - buscar nome do office;
  - buscar planos ativos;
  - buscar histórico de cobrança;
  - cancelar assinatura;
  - ativar assinatura.
- Corrigidos casts de retorno para `typecheck`.

Status: concluído.

### 6.3 Coupons

- Criado `src/features/admin/services/couponManagementService.ts`.
- Movidas operações:
  - buscar office do usuário;
  - listar cupons;
  - listar serviços;
  - buscar regras de desconto;
  - ativar/desativar cupom;
  - criar cupom.
- Ajustado `CreateCouponInput` para aceitar `is_active`.

Status: concluído.

### 6.4 Products

- Criado `src/features/admin/services/productsService.ts`.
- Movidas operações:
  - resolver office;
  - listar preços de serviços;
  - atualizar preços.
- Corrigidos casts do retorno com relação `services(...)`.

Status: concluído.

### 6.5 Zelle Payments

- Criado `src/features/admin/services/zellePaymentsPageService.ts`.
- Movidas consultas de pagamentos Zelle e Stripe/orders por status.

Status: concluído.

### 6.6 Checkout Success

- Criado `src/features/payments/services/checkoutSuccessService.ts`.
- Movidas operações:
  - buscar usuário atual;
  - buscar processo do pedido;
  - encontrar processo de mentoria ativo;
  - buscar estado de checkout do user service;
  - atualizar user service após checkout.

Status: concluído.

### 6.7 Case Onboarding

- Criado `src/features/process/services/caseOnboardingService.ts`.
- Movidas operações:
  - upload de documento;
  - URL pública de documento;
  - busca de workflow review.
- Ajustado retorno de `steps` e `reviews` para compatibilidade com a página.

Status: concluído.

---

## 7. Refatorações Alta - COS/RFE/Motion

### 7.1 RFEWorkflow

- Removidas chamadas diretas a Supabase/storage.
- Passou a usar:
  - `onboardingStorageService`;
  - `cosOnboardingService`.
- Movidas operações:
  - preço ativo do serviço;
  - upload de carta/documentos;
  - URL pública de arquivos.

Status: concluído.

### 7.2 MotionWorkflow

- Removidas chamadas diretas a Supabase/storage.
- Passou a usar:
  - `onboardingStorageService`;
  - `cosOnboardingService`.
- Movidas operações:
  - contato da conta;
  - preço ativo;
  - upload de documentos;
  - URL pública de pacote final.

Status: concluído.

### 7.3 COSOnboardingPage

- Criado `src/features/onboarding/services/cosOnboardingService.ts`.
- Movidas operações:
  - buscar processo COS por ID;
  - buscar último processo COS;
  - validar child recovery process;
  - buscar usuário atual;
  - upload de documentos via service.

Status: concluído.

---

## 8. Refatorações Alta - Checkout E Processo Cliente

### 8.1 CheckoutPage

- Criado `src/features/payments/services/checkoutPageService.ts`.
- Movidas operações:
  - logs de checkout;
  - resolução de office pelo processo;
  - status de assinatura do office;
  - logo/nome do office;
  - service/preço customizado.

Status: concluído.

### 8.2 OfficeCheckoutPage

- Reutilizado `checkoutPageService`.
- Movidas operações:
  - dados públicos do office;
  - métodos de pagamento públicos;
  - preço customizado;
  - logs de checkout.

Status: concluído.

### 8.3 ProcessDetailPage Cliente

- Criado `src/features/process/services/processDetailService.ts`.
- Movidas operações:
  - buscar processo do cliente;
  - buscar child process;
  - buscar recovery children;
  - buscar office brand;
  - realtime subscription do processo.

Status: concluído.

---

## 9. Refatorações Alta - Admin

### 9.1 ProcessesPage Admin

- Criado `src/features/admin/services/adminProcessesService.ts`.
- Movidas operações:
  - logs de processo;
  - office do admin/staff;
  - processos por office;
  - processos por clientes vinculados ao office;
  - listagem master;
  - dados de perfil do cliente;
  - nome do serviço.

Status: concluído.

### 9.2 CompanyProfilePage

- Criado `src/features/admin/services/companyProfileService.ts`.
- Movidas operações:
  - buscar office por owner;
  - verificar conflito de slug;
  - upload de logo;
  - atualizar logo;
  - criar/atualizar office;
  - vincular usuário ao office;
  - desativar produtos ao criar office.
- Ajustado `logoUrl` para evitar `unknown` em `img.src`.

Status: concluído.

### 9.3 RevenuePage

- Criado `src/features/admin/services/revenuePageService.ts`.
- Movidas operações:
  - nomes de offices;
  - pagamentos Zelle pendentes;
  - withdrawals;
  - payout settings;
  - orders aprovados;
  - inferência de office por owner;
  - update de `office_id` em orders;
  - update de status de payment;
  - chamada da function `withdrawals`.

Status: concluído.

---

## 10. Refatorações Alta - Preparação Final F1/B1B2

### 10.1 F1FinalPreparationStep

- Criado/reutilizado `src/features/onboarding/services/finalPreparationService.ts`.
- Movidas operações:
  - buscar office do processo;
  - buscar mentoria comprada;
  - buscar consultoria comprada;
  - carregar `step_data` atualizado;
  - carregar preços dos planos;
  - incrementar sessões agendadas;
  - reportar resultado da entrevista;
  - buscar compra mais recente;
  - criar/reusar conversa de suporte;
  - inserir mensagem inicial de suporte.

Status: concluído.

### 10.2 B1B2FinalPreparationStep

- Reutilizado `finalPreparationService`.
- Movidas operações:
  - buscar office do processo;
  - buscar mentoria comprada;
  - buscar consultoria comprada;
  - detectar consultoria pelas compras do processo;
  - carregar `step_data` atualizado;
  - carregar preços dos planos, incluindo consultoria pós-negativa;
  - incrementar sessões agendadas;
  - reportar resultado da entrevista;
  - criar/reusar conversa de suporte;
  - inserir mensagem inicial de suporte.

Status: concluído.

---

## 11. Correções De Typecheck

Correções aplicadas durante as refatorações:

- `NotificationProvider`: casts explícitos para rows normalizados.
- `RichEditor`: uso de `{ emitUpdate: false }`.
- `RevenuePage`: inclusão de `processingFeeAmount` no tipo local.
- `notify.ts`: inclusão de `title` e `body` no payload de notificação.
- `SignUpPage`: import de `useLocale`.
- `useCustomerChats`: tipagem de `Json` e ajuste de assinatura de elegibilidade.
- `HomePage`: inclusão de chaves `nextStep1` e `nextStep2`; ajuste do cast de traduções.
- `localizeNotificationContent`: compatibilidade com templates novos e labels legados.
- `COSOnboardingPage`: remoção de comparação impossível no toast final.
- `ProcessDetailPage` cliente: adaptação de templates de workflow para o tipo esperado.
- `UserService`: inclusão de `negativa`.
- `MaintenancePage`: inclusão de namespace `maintenance`.
- `adminCustomerService`: status convertido para string antes de comparação.
- `productsService`: cast de retorno com relação `services(...)`.
- `subscriptionPageService`: cast de retorno com relação `subscription_plans(...)`.
- `InteractionLogsPage`: import de `toast`.
- `CompanyProfilePage`: `logoUrl` normalizado como string.

Status: concluído.

---

## 12. Análise E Limpeza De Comentários

### 12.1 Análise Dos Comentários

Foi feita análise dos comentários no código com foco em:

- comentários úteis que explicam decisão técnica;
- comentários de seção em excesso;
- comentários óbvios de UI;
- comentários narrativos deixados por refactor;
- código comentado;
- comentários de regra de negócio.

Conclusão da análise:

- Comentários técnicos em `AuthProvider`, `NotificationProvider`, `processOps` e alguns services foram considerados úteis e mantidos.
- Comentários decorativos de seção foram considerados aceitáveis, mas não prioritários para remoção global.
- Comentários obsoletos/código morto foram priorizados para remoção.

Status: concluído.

### 12.2 Comentários Removidos

Removido código morto comentado em:

- `src/features/process/pages/CaseOnboardingPage.tsx`

Removidos comentários narrativos/obsoletos em:

- `src/features/onboarding/f1/pages/F1OnboardingPage/steps/F1FinalPreparationStep.tsx`
- `src/features/onboarding/b1b2/pages/B1B2OnboardingPage/steps/B1B2FinalPreparationStep.tsx`
- `src/features/payments/pages/OfficeCheckoutPage/index.tsx`

Exemplos de comentários removidos:

- `Fresh Data State`
- `Mentorship State`
- `Consultation State`
- `Mentoria now starts via support chat...`
- `New Interview Result Logic`
- `Main Tools Section - Always Visible`
- `Original content moved here`
- `outcome logic remains...`
- `Lógica de Telas Pós-Resultado`
- `Logs de Interação...`

Status: concluído.

---

## 13. Tabela Final De Status

| Task | Dificuldade | Status |
|---|---:|---|
| `PaymentMethodsSettingsPage` | Média/Alta | Concluído |
| `ProductsPage` | Média/Alta | Concluído |
| `SubscriptionPage` | Média/Alta | Concluído |
| `CouponsPage` | Média/Alta | Concluído |
| `ZellePaymentsPage` | Média/Alta | Concluído |
| `CheckoutSuccessPage` | Média/Alta | Concluído |
| `CaseOnboardingPage` | Média/Alta | Concluído |
| `useCOSOnboardingPage` | Baixa/Média | Concluído |
| `RFEWorkflow.tsx` | Alta | Concluído |
| `MotionWorkflow.tsx` | Alta | Concluído |
| `COSOnboardingPage/index.tsx` | Alta | Concluído |
| `CheckoutPage` | Alta | Concluído |
| `OfficeCheckoutPage` | Alta | Concluído |
| `ProcessDetailPage` cliente | Alta | Concluído |
| `ProcessesPage` admin | Alta | Concluído |
| `CompanyProfilePage` | Alta | Concluído |
| `RevenuePage` | Alta | Concluído |
| `F1FinalPreparationStep` | Alta | Concluído |
| `B1B2FinalPreparationStep` | Alta | Concluído |
| `ProcessDetailPage` admin | Muito alta | Pendente |

---

## 14. Validações Executadas

### 14.1 Typecheck

Comando:

```bash
npm run typecheck
```

Resultado:

- passou sem erros após os ajustes finais.

### 14.2 Buscas Direcionadas

Foram feitas buscas nos arquivos refatorados por:

- `supabase`
- `.from(`
- `.rpc(`
- `.storage.`
- `functions.invoke`
- `getSupabaseClient`
- `fetch(`

Resultado:

- páginas refatoradas ficaram sem chamadas diretas reais de API/Supabase;
- ocorrências restantes reportadas eram falsos positivos como `Array.from` ou `refetch()` do React Query.

---

## 15. Pendência

Ainda pendente:

- `src/features/admin/pages/ProcessDetailPage/index.tsx`

Motivo:

- página muito grande, com alta concentração de regra de negócio, storage, queries e fluxos administrativos;
- classificada como dificuldade muito alta;
- deve ser tratada em uma etapa separada para reduzir risco de regressão.
