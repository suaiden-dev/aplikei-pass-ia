import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const sb = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await sb.from('user_services').select('id').in('service_slug', ['extensao-status', 'troca-status', 'changeofstatus']).order('updated_at', { ascending: false }).limit(1); if (data && data.length > 0) { console.log('Updating ' + data[0].id); await sb.from('user_services').update({ status: 'EOS_MOTION_IN_PROGRESS' }).eq('id', data[0].id); console.log('Done'); } } run();
